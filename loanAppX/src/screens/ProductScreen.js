import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class ProductScreen extends Component {
    render() {
        return (
            <View>
                <Text> test ProductScreen </Text>
            </View>
        )
    }
}

const styles = StyleSheet.create({})
